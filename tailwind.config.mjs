// tailwind.config.mjs
// NOTE : En Tailwind CSS v4, la configuration du thème se fait directement
// dans src/styles/global.css via les directives @theme et @plugin.
// Ce fichier est conservé pour référence — il n'est PAS chargé par le build.
//
// Voir : src/styles/global.css pour la configuration active.
//
// Correspondances v3 → v4 :
//   theme.extend.colors     → @theme { --color-* }
//   theme.extend.fontFamily → @theme { --font-family-* }
//   plugins: [typography]   → @plugin "@tailwindcss/typography"

export default {};
